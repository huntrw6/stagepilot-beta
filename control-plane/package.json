{
  "name": "stagepilot-beta-control-plane",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "acceptance:live": "node scripts/live-acceptance.mjs",
    "acceptance:baseline": "node scripts/residue-check.mjs baseline",
    "acceptance:residue": "node scripts/residue-check.mjs verify",
    "acceptance:sweep": "node scripts/sweep-residue.mjs",
    "validate:deployment": "node scripts/deployment-config.mjs validate"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "typescript": "^5.9.3",
    "vitest": "^5.0.0"
  }
}
