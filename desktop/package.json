{
  "name": "@stagepilot/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri v2 shell for the StagePilot dashboard",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
