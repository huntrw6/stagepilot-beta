{
  "name": "@stagepilot/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri v2 shell for the StagePilot dashboard",
  "scripts": {
    "dev": "tauri dev",
    "build": "npm run build:sidecar && tauri build --config src-tauri/tauri.release.conf.json",
    "build:sidecar": "python ../scripts/build_backend_sidecar.py"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
